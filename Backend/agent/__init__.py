"""
Agentic layer: startup discovery, job matching, auto-apply and cold outreach.

Sits beside `api/` (the dashboard backend) rather than inside it — both are
imported as top-level packages with Backend/ on sys.path.
"""
