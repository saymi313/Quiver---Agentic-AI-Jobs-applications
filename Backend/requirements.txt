# Company crawl (CSV pipeline)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Resume engine
jinja2>=3.1.0
pyyaml>=6.0.0

# Dashboard API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0

# Resume parsing / generation
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
python-docx>=1.1.0
reportlab>=4.1.0          # non-LaTeX PDF fallback only

# Agent: data store
pymongo[srv]>=4.8.0       # MongoDB backend; falls back to bundled SQLite if unset

# Agent: startup research, auto-apply, cold outreach
playwright>=1.45.0        # then run once:  python -m playwright install chromium
dnspython>=2.6.0          # MX lookups for email verification
